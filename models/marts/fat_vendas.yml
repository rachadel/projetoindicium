
version: 2

models:
  - name: fat_vendas
    description: Essa é a fato vendas
    columns:
      - name: sk_venda	
        description: essa é a coluna chave surrogate key da tabela
        tests:
          - unique
          - not_null
